#!/bin/bash
# Support custom host/port via SHELLPROXY_HOST and SHELLPROXY_PORT.
# Defaults: 127.0.0.1:7890
PROXY_HOST="${SHELLPROXY_HOST:-127.0.0.1}"
PROXY_PORT="${SHELLPROXY_PORT:-7890}"

echo "http://${PROXY_HOST}:${PROXY_PORT}"
